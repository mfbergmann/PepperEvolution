[tool.pytest.ini_options]
asyncio_mode = "auto"
# `pytest tests/` (as CI runs it) must find the src/ package without `python -m pytest`
pythonpath = ["."]
testpaths = ["tests"]

[tool.black]
line-length = 120

[tool.mypy]
ignore_missing_imports = true
no_strict_optional = true
