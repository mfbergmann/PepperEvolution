# Optional: voice input on the host (STT_BACKEND=sherpa or whisper).
#   pip install -r requirements-voice.txt
# Both backends run on the CPU; no GPU and no extra API key needed.

numpy>=1.26

# Streaming recogniser with built-in endpointing (recommended: STT_BACKEND=sherpa).
# Models are downloaded separately, e.g.
#   https://github.com/k2-fsa/sherpa-onnx/releases/download/asr-models/sherpa-onnx-nemotron-speech-streaming-en-0.6b-560ms-int8-2026-04-25.tar.bz2
#   (or the smaller sherpa-onnx-streaming-zipformer-en-2023-06-26 for a quiet room or a slow machine)
# then STT_MODEL=/path/to/the unpacked model directory
sherpa-onnx>=1.13

# Per-utterance recogniser (STT_BACKEND=whisper, STT_MODEL=base|small|...); models download on first use.
# faster-whisper>=1.2   (also used by scripts/compare_stt.py --whisper)
